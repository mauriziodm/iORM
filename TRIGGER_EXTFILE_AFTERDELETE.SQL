SET TERM ^ ;

CREATE TRIGGER EXTFILE_AFTERDELETE FOR EXTFILE
ACTIVE AFTER DELETE POSITION 0
AS
BEGIN

  /* Elimina gli allegati relativi al documento eliminato */
  DELETE FROM ALLEGATI A  
  WHERE A.TIPODOC = 'Doc.esterno'
    AND A.REGDOC  = OLD.REGISTRO
    AND A.NUMDOC  = OLD.CODICE
    AND A.DATADOC = OLD.DATA;

END^

SET TERM ; ^

