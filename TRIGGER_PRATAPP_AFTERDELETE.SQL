CREATE TRIGGER PRATAPP_AFTERDELETE FOR PRATAPPARECCHI
ACTIVE AFTER DELETE
AS
BEGIN

  /* Elimina le operazioni pianificate dell'impianto e dell'apparecchio */
  DELETE FROM OPPRATICA OP WHERE OP.IDAPPARECCHIOPRAT = OLD.ID;

END
