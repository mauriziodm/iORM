CREATE TRIGGER PRVORDCL_DOCCORRELATI_BI FOR PRVORDCL_DOCCORRELATI
ACTIVE BEFORE INSERT
AS
  DECLARE LAST_ID INTEGER;
BEGIN


  /* Se l'ID non è già assegnato lo assegna */
  IF (NEW.ID IS NULL) THEN NEW.ID = GEN_ID(PRVORDCL_DOCCORRELATI, 1);

END
