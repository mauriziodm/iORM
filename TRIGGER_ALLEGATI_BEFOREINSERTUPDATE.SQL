CREATE TRIGGER ALLEGATI_BEFOREINSERTUPDATE FOR ALLEGATI
ACTIVE BEFORE INSERT OR UPDATE
AS
BEGIN

  /* Se le note sono vuote ci copia sopra il nome del file */
  IF (COALESCE(NEW.NOTE,'') = '') THEN NEW.NOTE = COALESCE(NEW.FILE_NAME,''); 

END
