CREATE TRIGGER CLIENTI_AFTERDELETE FOR CLIENTI
ACTIVE AFTER DELETE POSITION 0
AS
BEGIN

  /* Elimina listini secondari del fornitore */
  DELETE FROM LISTFORN LF WHERE LF.CODICEFORNITORE = OLD.CODICE;

END
