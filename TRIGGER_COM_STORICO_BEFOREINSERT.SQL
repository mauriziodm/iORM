CREATE TRIGGER COM_STORICO_BEFOREINSERT FOR COM_STORICO
ACTIVE BEFORE INSERT POSITION 0
AS
BEGIN

  /* Assegna l'ID se non è già stato assegnato */
  IF (NEW.ID IS NULL) THEN BEGIN
    NEW.ID = GEN_ID(GEN_ID_COM_STORICO,1);          
  END

END
