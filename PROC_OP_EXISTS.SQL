SET TERM ^ ;

CREATE PROCEDURE OP_EXISTS (IN_CODICE VARCHAR(25))
  RETURNS (OUTVAL CHAR(1))
AS
BEGIN

  IF (EXISTS(SELECT OP.CODICE FROM OP WHERE OP.CODICE=:IN_CODICE))
    THEN OUTVAL = 'T';
    ELSE OUTVAL = 'F';

  SUSPEND;
  EXIT;

END^

SET TERM ; ^


