CREATE TRIGGER PRVORDCL_FE_BEFOREINSERT FOR PRVORDCL_FE
ACTIVE BEFORE INSERT POSITION 0
AS
BEGIN

  /* Assegna l'ID se non è già stato assegnato */
  IF (NEW.ID IS NULL) THEN BEGIN
    NEW.ID = GEN_ID(GEN_ID_PRVORDCL_FE,1);          
  END

END